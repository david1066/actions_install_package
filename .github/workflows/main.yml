name: Test
on: [push]

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps: 
      - name: ls
        run: ls -al
      - name: nginx
        run: |
          echo "Hola mundo" >> holat.txt
          cat holat.txt
